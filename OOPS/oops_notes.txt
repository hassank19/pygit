object bundle of related attribute and methods - object has attributes, like a phone's version, color etc, and method is its capability. Methods are functions that belong to an object.

Classes are used to design an object (blueprint), class is needed to create many objects.

Class (The Blueprint): An abstract template that defines what characteristics (attributes/data) and actions (methods/functions) a certain type of item will have. It does not occupy memory space for actual data.Object (The Real Thing): The actual physical presence built from that template. It occupies memory space and contains real values. //object is an instance of a class


Class = Struct, Objects = array of structs

To construct an object, we need a special method called the constructor.

for ex, to create an object, 
class Car: ------> //this is our class, the **blueprint**
   def __init__(self, model, year, color, for_sale): //we need this method (called the constructor), self refers to the object we are currently working with
	self.model = model
	self.year = year
	self.color = color
	self.for_sale = for_sale 

//now making an object
car1 = Car("Mustang", 1998, "red", false) //car1 is the object name
print(car1.object)
print(car1.year) etc

we can have more methods in a class like: 
class Car:
   def __init__(self, model, year, color, for_sale): 
	self.model = model
	self.year = year
	self.color = color
	self.for_sale = for_sale 

	def drive(self):
		print(f"You are driving {self.model}")
	def stop(self):
		print(f"You stopped {self.model}")

Instance variables vs Class variables, instance variables are defined inside of a constructor like:
   def __init__(self, model, year, color, for_sale): 
	self.model = model
	self.year = year
whereas,
Class variables are shared among all instances (objects) of a class, -->They are defined outside the constructor
Instance variables = Changes as instances, Class variables = shared by all objects of that class (common for all instances)

Class variables are accessed using the class' name, ex:
car1.name //for instance variables
Car.num --> Car is name of class

-------Inheritance in OOPs--------
Inheritance allows a class to inherit methods and attributes from another class
class parent(self, name):
	self.name = name

	def species(self):
		print("Human")
	
class child(parent):
	pass

child1 = child("Alex")
child1.species() --->prints Human, it inherited from parent class
#-> refer to Inheritance.py

---------Multiple Inheritance----------
Inherit from more than one parent
_______________________________________
Abstract classes, a class that cannot be instantiated on its own, it is meant to be subclassed #supposed to have children
Cant create an object from an abstract class
They have abstract methods whhich r declared but have no implementation
It has abstract methods supposed to be inherited by the children

The methods of the abstract class are defined or implemented in the child classes, 
without defining them we cannot make objects from the child classes
_______________________________________
Aggregation = It means two objects that have a 'has a' relationship, ex: A library has a book
Composition = It means two objects have a relationship where one owns the another
_______________________________________
